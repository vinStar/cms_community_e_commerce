import {
  getStorage,
  setStorage,
  removeStorage
} from './storage';
import { postData } from './postData';
import { authorization } from './authorization';
import rest from './rest';

export default {
  getStorage,
  setStorage,
  removeStorage,
  postData,
  authorization,
  rest
}
