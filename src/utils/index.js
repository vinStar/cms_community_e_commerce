import {
  getStorage,
  setStorage,
  removeStorage
} from './storage';
import { postData } from './postData';
import { authorization } from './authorization';

export default {
  getStorage,
  setStorage,
  removeStorage,
  postData,
  authorization
}
